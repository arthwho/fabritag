# Correção do commit anterior

O Firmware ainda enviava sensor_id como número/valor sem aspas e o campo do WiFiManager só aceitava 10 caracteres, então um UUID era truncado. Além disso, qualquer HTTP positivo era mostrado como DB Sync: SUCCESS, até 400/500.

- sensorId agora começa vazio, o campo virou Sensor UUID e aceita UUID completo.
- JSON agora envia "sensor_id":"uuid...", heartbeat também envia string, o ESP32 só mostra sucesso para HTTP 2xx, e abre o portal automaticamente se o UUID salvo estiver vazio/inválido.
- /api/tag_event agora trata JSON inválido/UUID inválido como 400, em vez de virar erro genérico.